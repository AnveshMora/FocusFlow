import { Request, Response } from 'express';

export const getTodayAnalytics = async (req: Request, res: Response) => {
  res.json({});
};

export const getWeeklyAnalytics = async (req: Request, res: Response) => {
  res.json({});
};

export const getStreakAnalytics = async (req: Request, res: Response) => {
  res.json({});
};

export const getHeatmapAnalytics = async (req: Request, res: Response) => {
  res.json({});
};
